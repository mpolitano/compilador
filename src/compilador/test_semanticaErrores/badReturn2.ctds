/*en este test probamos que el tipo del return debe ser igual que el declarado*/

class badReturn2{
	
	int isTwo(int a){
		if (a==2) {
			return true;
		}else{
			return false;
		}
	}

int main(){
	int aux;
	boolean boolean_aux;
	aux=2;
	boolean_aux= isTwo(aux);  //isTwo retorna un int y boolean_aux es de tipo boolean
	externinvk("printf",int, boolean_aux);

	return 1;
}

}