/*en este test intento devolver un valor en una funcion definida como void, esto no esta permitido, debe fallar*/
class notVoid{

	void next(int a){ //funcion void, no retorna nada
		a+=1;
		return a; //aqui hay un error, estamos retornando en una funcion void
	}

void main () {
	int a;
	a=10;
	next(a);
	externinvk("printf", int, a);
}

}
