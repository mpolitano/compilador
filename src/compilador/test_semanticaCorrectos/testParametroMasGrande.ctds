/*Test en el que redefino el valor de la variable mas grande que me pasen
el mismo no deberia fallar, ya que lo redefino en otro bloque
*/
class testParametroMasGrande{
	
	int masGrande(int x, int y) {
		if (x>y){
			int a;
			a=1;		
			return a;
		}else{
			int b;
			b=2;
			return b;		
		}
	}

	int main(){
		int a;
		int b = 3;
		a = 2;
		externinvk("printf", int , masGrande(a,b));	
	} 
}
