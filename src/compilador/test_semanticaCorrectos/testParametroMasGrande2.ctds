/*Test en el que redefino el valor de la variable mas grande que me pasen
el mismo no deberia fallar, ya que, al estar dentro del if, es un bloque distinto a los parametros
*/
class testParametroMasGrande2{
	
	int masGrande(int a, int b) {
		if (a>b){
			int a;
			a=1;		
			return a;
		}else{
			int b;
			b=2;
			return b;		
		}
	}

	int main(){
		int a;
		int b = 3;
		a = 2;
		externinvk("printf", int , masGrande(a,b));	
	} 
}
