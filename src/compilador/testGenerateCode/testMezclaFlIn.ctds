class maxComunDivisor{	
int main(){
int a,b;
float c;
		c=3.0;
		a=-3;
		b=3;
		if(a==c){//Ver como imprimirlo pero anda bien el compare y convertir.
			externinvk("printf", void, "%d == %d \n",a,c);		
		}else{
			externinvk("printf", void, "%d != %d \n",a,c);
		}

}
	
}