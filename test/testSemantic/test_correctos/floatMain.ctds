/*test para probar que main puede ser float. */
class floatMain{

	float div(float a, float b){
		return (a/b);	
	}

	float main(){ //definimos main como float
		float a;
		float b;
		a=1.00;
		b=2.00;
		externinvk("printf", float, div(a,b));

		return 1.100;
	}

}
