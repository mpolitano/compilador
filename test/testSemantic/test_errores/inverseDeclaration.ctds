/*test para probar que una variable no puede ser usada sin ser definida previamente,
la variable b sera definida despues de una asignacion en b, lo cual, debe fallar*/
class inverseDeclaration{

	int masChico (int a, int b){
		if (a<b){
			int a;
			a=1;
			return a;		
		}else{
			int b;
			b=2;
			return b;
		}
	}

int main(){
	int a; 
	a=1;	
	b=2; //queremos asignar el valor 2 a una variable que no fue definida
	int b;
	externinvk("printf", int, masChico("printf",int,masChico(a,b)));

	return 1;
}


}
